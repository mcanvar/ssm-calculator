import { logCurrentColor } from "../background";

test('Saved color should be valid 7 digit HEX color with dash.', () => {
  // TODO: implement the test
  expect(logCurrentColor()).toBe('#000000');
})